<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.uottawa.gcc_final">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GCC"
        tools:targetApi="34">

        <activity
            android:name="com.uottawa.gcc_final.WelcomeActivity"
            android:exported="false">
        </activity>

        <activity
            android:name="com.uottawa.gcc_final.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.uottawa.gcc_final.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".EditEventActivity">
        </activity>

        <activity
            android:name=".DeleteEventActivity">
        </activity>


    </application>

</manifest>
